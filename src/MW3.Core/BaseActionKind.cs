namespace MW3.Core;

/// <summary>
/// One kind of action a base's owner can take on it. <see cref="Upgrade"/> is the only one this
/// phase; convert joins it in FR-5.
/// </summary>
public enum BaseActionKind
{
    Upgrade,
}
