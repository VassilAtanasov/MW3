# RE-AUTHORED at phase 6 FR-3 (issue #87), for two separate reasons - and the first one is a real
# defect in this script that FR-3 did not cause and does not fix:
#
#  1. THIS SCRIPT'S SETUP NO LONGER RUNS. Its frame-20 tap at (0.176563, 0.281944) was the
#     convert-to-tower button; phase 6 FR-1 gave the action menu a second convert entry (D-43's
#     explicit target type) and the tap no longer lands on it. Base 0 therefore stays a Producer,
#     the tower never exists, it never shoots anything, and the "Human's Morale should read
#     strictly positive" expectation below is simply not met - Human reads 0. This was already true
#     on main at 430c999, before FR-3 existed (verified by running this script there). The same
#     stale tap breaks army-shrinking-early.txt and army-shrinking-late.txt, which share the setup.
#     Filed as follow-up #90 rather than fixed here: re-aiming three scripts at the new menu is FR-5's
#     territory, since FR-5 is the feature that settles what those buttons are labelled and where.
#  2. FR-3 made the forge term live, and separately moved this run's AI-side numbers. The AI holds
#     the map's centre forge (base 6) from early on, so from that point it attacks at ForgeTable's
#     150% and defends at 125%; by the end of the run it has taken the neutral tower (base 7) and
#     upgraded base 3, neither of which it managed before. Ai morale 60 -> 100. Nothing on the
#     Human side moves - the human's own numbers are identical to main's, because the human owns no
#     forge in this run.
#
# The description below is kept as the record of what this script was written to prove. It becomes
# true again once its setup is repaired.
#
# Same setup as army-shrinking-late.txt (human converts base 0 to a tower), waited further so the
# tower has finished destroying at least one AI army that flew within its range - the same
# mechanism army-shrinking-early.txt/army-shrinking-late.txt already prove for garrison count, now
# read off the Morale: line instead. Run with --time-scale 10 (see convert-pending.txt for why).
#
# Every unit the tower destroys is +10 to Human (MoraleTable.AttackingUnitDestroyedGain, the
# tower's owner) and -10 to Ai (MoraleTable.AttackingUnitDiedLoss, the army's owner) - opposite
# directions from the same shot. Cross-check against the dump's Army lines: an army present earlier
# in the run (e.g. army-shrinking-early.txt's Army 30) either shows a lower Count= or has vanished
# outright (destroyed and removed from ArmiesInFlight, per FR-1's tower-fire criterion); the number
# of units it lost, times 10, is the swing this script exists to make visible - Human's Morale
# should read strictly positive, and if Ai has not separately banked points elsewhere in the same
# run (a capture, an upgrade), the two sides move by matching magnitudes in opposite directions.
0 down 0.500000 0.591667
5 up 0.500000 0.591667
10 down 0.120000 0.500000
15 up 0.120000 0.500000
20 down 0.176563 0.281944
25 up 0.176563 0.281944
320 down 0.120000 0.500000
325 up 0.120000 0.500000
330 down 0.075000 0.281944
335 up 0.075000 0.281944
700 wait
