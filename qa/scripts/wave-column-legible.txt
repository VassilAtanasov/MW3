# FR-2 (D-56): re-homed onto --map small. The opening Play tap (two lines) was deleted and
# every remaining frame number reduced by 5, since the flag starts the match at frame 0 where
# the tap started it at frame 5. Proved, not assumed: this script's --dump-state output is
# byte-identical to its own output on main, aside from the two centre-line slots (bases 6, 7)
# the retired eight-slot board carried and Small does not.
# Tap Play, select 100% on the strength control, then immediately drag from the human base all the
# way to the AI's base - the longest, purely-horizontal edge on the map, chosen so the wave
# spacing (0.05 map units - ArmySpeedUnitsPerTick * SendWaveCalculator.WaveIntervalTicks) projects
# onto its full pixel value along the viewport's width rather than being foreshortened by a
# diagonal edge (FR-4, D-36). The starting garrison of 10 splits into a two-wave column - 8, then
# the 2-unit remainder - exactly as send-wave-column.txt's does, but travelling this longer edge
# keeps both waves clear of the source and target bases at frame 130 (tick 43), so the tapered
# radii (0.08 for wave 1, 0.04 for wave 2) and the per-wave counts are both legible mid-flight
# rather than overlapping a base circle.
5 down 0.033203 0.670139
10 up 0.033203 0.670139
15 down 0.120000 0.500000
20 up 0.880000 0.500000
125 wait
