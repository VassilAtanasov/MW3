# FR-2 (D-56): re-homed onto --map small. The opening Play tap (two lines) was deleted and
# every remaining frame number reduced by 5, since the flag starts the match at frame 0 where
# the tap started it at frame 5. Proved, not assumed: this script's --dump-state output is
# byte-identical to its own output on main, aside from the two centre-line slots (bases 6, 7)
# the retired eight-slot board carried and Small does not.
# FR-2 regression proof: with both players at morale 0 every composed index is exactly identity
# (CombatResolverTests), so this script is deliberately byte-identical in its input to the
# pre-existing phase-2 army-arrival.txt - hold long enough for the human base to grow past twice
# the neutral's garrison, then drag to the nearest neutral and hold long enough (~17 ticks) for the
# army to land and capture it. Since FR-2 changed CombatResolver's index scale (percent to basis
# points) and both call sites that compose it (Match.ResolveArrival, AiBrain's predictions), this
# proves the observable end-to-end result - which base ends up owned by whom, with what garrison -
# is bit-identical to before this feature, exactly as docs/morale/ARCHITECTURE.md's morale-0
# baseline guarantee requires. If this script's dump ever needs a different budget than
# army-arrival.txt's, that is a signal the morale-0 identity was broken - investigate rather than
# re-baseline (the standing rule since FR-3a).
195 down 0.120000 0.500000
200 up 0.350000 0.250000
395 wait
