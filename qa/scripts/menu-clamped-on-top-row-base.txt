# FR-2 (D-56): re-homed onto --map small. The opening Play tap (two lines) was deleted and
# every remaining frame number reduced by 5, since the flag starts the match at frame 0 where
# the tap started it at frame 5. Proved, not assumed: this script's --dump-state output is
# byte-identical to its own output on main, aside from the two centre-line slots (bases 6, 7)
# the retired eight-slot board carried and Small does not.
# Tap Play, then two quick sends from the human base to the top-row neutral at y=0.25 - the first
# ties it down to zero (5 v 5, stays neutral), the second (launched before the first lands) captures
# it a couple of ticks later - then open that base's action menu. Exercises the viewport clamp for a
# base near the top edge rather than the middle (FR-2), now with three buttons (Upgrade,
# Convert:Tower, Convert:Forge) once BaseType gained Forge (phase 6 FR-1, D-48). Kept short (well
# under the AI's own 17-tick reach to the nearest base) so the AI's first decision (tick 20) cannot
# land before the script ends.
5 down 0.120000 0.500000
10 up 0.350000 0.250000
15 down 0.120000 0.500000
20 up 0.350000 0.250000
165 down 0.350000 0.250000
170 up 0.350000 0.250000
