# .NET
bin/
obj/
*.user
*.suo
.vs/
artifacts/
TestResults/
*.db
*.db-shm
*.db-wal

# Quality gate success stamp (local cache, machine-specific)
.gate-stamp

# Coverage output written by the gate's test step
.coverage/

# MW3.Server's per-match FR-6 logs (default logs/ under its content root, or wherever --log-dir points)
logs/

# Editors / OS
.idea/
*.swp
.DS_Store
Thumbs.db

# Local-only Claude settings (machine-specific overrides)
.claude/settings.local.json

# Secrets (WORKFLOWY_API_KEY lives here)
.env
.env.local
